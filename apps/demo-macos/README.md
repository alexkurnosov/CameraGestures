# Demo App — macOS

Minimal SwiftUI app for macOS: AVCaptureSession + detected-gesture overlay.
Populated in Stage 7 — **only if the Stage 3 macOS go/no-go spike passes**.
If macOS is deferred, this directory will be removed and noted in `core/README.md`.
